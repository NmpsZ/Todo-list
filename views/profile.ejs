<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="../css/profile.css">
</head>

<body>
    <%-include('navbar') %>
    <div class="profile-container">
        <div class="profile-card">
            <form action="/profile/edit" method="POST">
                <input type="hidden" name="edit_profile_id" value="<%= data._id %>">
                <div class="profile-image">
                    <img src="<%= data.profileImage %>" alt="Profile Picture">
                </div>
                <h2 class="profile-name"><%= data.username %></h2>
                <p class="profile-email">Email: <%= data.email %></p>
                <button type="submit" class="profile-edit-button">Edit Profile</button>
            </form>
        </div>
    </div>
</body>


</html>
